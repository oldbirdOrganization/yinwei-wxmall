<view class="form-item">
  <view class="form-label">选择服务时间</view>
  <view class="form-input">
    <picker value="{{index}}" bindchange="bindValue" range="{{range}}">
      <view class="picker">{{serviceTime || "选择服务时间"}}</view>
    </picker>
    <view class="form-unit"></view>
  </view>
</view>